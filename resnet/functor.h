#pragma once
#include "descriptor.h"
#include "global.h"
// static T* FIX(float* ptr){
//     return static_cast<T*>(ptr);
// }
// static const T* FIX(const float* ptr){
//     return static_cast<const T*>(ptr);
// }
// class FunctorBase {
//   public:
//     // FunctorBase() = default;
//     // virtual void forward(float* out, const float* in, const float* weight) = 0;
//     // virtual void backwardData(float* in_grad, const float* out_grad,
//     //                           const float* weight) = 0;
//     // virtual void backwardFilter(float* weight_grad, const float* out_grad,
//     //                             const float* in) = 0;
//     // virtual size_t weight_size() = 0;
//     // virtual size_t workspace_fwd() = 0;
//     // virtual size_t workspace_bwd_data() = 0;
//     // virtual size_t workspace_bwd_filter() = 0;
//     virtual ~FunctorBase() {}
// };

